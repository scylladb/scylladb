name: PR require backport label
on:
  pull_request:
    types: [opened, labeled, unlabeled, synchronize]
    branches:
      - master
      - next
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
  label:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
<<<<<<< HEAD
      - uses: mheap/github-action-required-labels@v5
||||||| parent of 99f4121cd8 (ci: pin GitHub Actions to commit SHAs and migrate to Node.js 24)
      - name: Wait for label to be added
        run: sleep 1m
      - uses: mheap/github-action-required-labels@v5
=======
      - name: Wait for label to be added
        run: sleep 1m
      - uses: mheap/github-action-required-labels@0ac283b4e65c1fb28ce6079dea5546ceca98ccbe # v5.5.2
>>>>>>> 99f4121cd8 (ci: pin GitHub Actions to commit SHAs and migrate to Node.js 24)
        with:
          mode: minimum
          count: 1
          labels: "backport/none\nbackport/\\d.\\d"
          use_regex: true
          add_comment: false

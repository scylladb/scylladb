type: Topology
pool_size: 4
cluster_size: 0
extra_scylla_config_options:
    authenticator: AllowAllAuthenticator
    authorizer: AllowAllAuthorizer

skip_in_release:
  - test_writes_to_previous_cdc_generations

skip_in_debug:
  - test_writes_to_previous_cdc_generations
